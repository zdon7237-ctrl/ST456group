# ST456 Final Report Outline

本文是最终报告写作大纲，不是最终报告草稿。  
正文主线以已经完成的 `E1-E5` 结果为核心证据；retrieval 已经跑完，但只放在 appendix 中做附加对照，不进入正文主结果表，也不改变正文主结论；human evaluation 本次不进入实验结果，只在 limitations / future work 中说明 deferred。

## Title

建议标题二选一：

- `Contextual Coherence in Sherlock Holmes Paragraph Continuation via GPT-2 Fine-Tuning`
- `A Comparative Study of Context Modeling and Fine-Tuning Strategies for Sherlock Holmes Paragraph Continuation`

本节应写什么：

- 题目直接点出任务：Sherlock Holmes paragraph continuation。
- 题目突出正文主线：context modeling、fine-tuning strategy、auxiliary objective。

本节不要写什么：

- 不要把 retrieval 放进标题。
- 不要把 human evaluation 放进标题。

## Abstract

本节应写什么：

- 用 1 段写清任务、方法、主结果、结论。
- 先交代任务：给定前 `k` 个段落，生成下一段。
- 再交代方法：以 `distilgpt2` 为 backbone，比较输入结构、上下文长度、fine-tuning strategy 和 auxiliary objective。
- 最后交代主结论：
  - `E3` 是最强主线配置；
  - `E2` 相比 `E1` 只有轻微改善；
  - `E4` 明显弱于 full fine-tuning；
  - `E5` 与 `E5W` 都没有优于 `E3`。

本节不要写什么：

- 不要在 abstract 中展开 retrieval appendix 结果。
- 不要在 abstract 中写 human evaluation。
- 不要在 abstract 中写过多具体数值，留给正文主结果表。

## 1. Introduction

本节应写什么：

- 介绍 paragraph continuation 的任务难点，重点强调 contextual coherence、character consistency、narrative continuity。
- 说明 Sherlock Holmes 语料为什么适合这个任务：同一 narrative world、重复角色、风格相对稳定。
- 明确本项目的主问题是 deep learning design choices，不是 retrieval。
- 列出正文允许的 4 组主比较：
  - E1 vs E2：输入结构
  - E2 vs E3：上下文长度
  - E3 vs E4：full vs LoRA
  - E3 vs E5：auxiliary objective
- 用一个简短段落预告 main findings：
  - structured input 只有轻微帮助；
  - longer context + structured full (`E3`) 最稳；
  - LoRA 在本设置下明显退化；
  - ranking auxiliary 没有带来收益。
- 结尾可点一句：retrieval was run only as an appendix-level comparison and does not change the mainline conclusions.

本节不要写什么：

- 不要把 retrieval 当作 main contribution。
- 不要在 introduction 里展开 appendix 数值。

## 2. Related Work

本节应写什么：

- 按 3 到 4 类相关工作组织：
  - GPT-2 / autoregressive language models
  - story generation / long-form coherence
  - LoRA and parameter-efficient fine-tuning
  - auxiliary objectives for conditional generation
- retrieval-related work 只点到为止，作为 appendix 背景即可。
- 结尾可以写一句：retrieval is treated as an appendix-level extension rather than the core contribution of this report.

本节不要写什么：

- 不要把 retrieval literature 展开成主章节。
- 不要让 related work 比 methodology 更长。

## 3. Methodology / Architecture

### 3.1 Task Formulation

本节应写什么：

- 明确任务是 conditional next-paragraph generation。
- 输入是前 `k` 个段落，输出是目标下一段。

本节不要写什么：

- 不要在这里展开实验结果。

### 3.2 Backbone

本节应写什么：

- backbone 为 `distilgpt2`。
- 简述选择理由：小模型、Colab 可训练、适合 controlled comparison。

### 3.3 Input Representation

本节应写什么：

- `plain` vs `structured`
- structured 输入如何加入段落标记

### 3.4 Context Window

本节应写什么：

- `k=2` vs `k=4`
- 强调这是上下文信息量的核心对比

### 3.5 Fine-Tuning Strategy

本节应写什么：

- full fine-tuning
- LoRA

### 3.6 Auxiliary Objective

本节应写什么：

- next-paragraph ranking objective
- `E5` 与 `E5W` 的区别只是 `aux_weight`

本节不要写什么：

- 不在正文 methodology 中展开 retrieval 机制细节。
- retrieval 细节留到 appendix A。

## 4. Training Methods and Experimental Setup

### 4.1 Dataset and Preprocessing

本节应写什么：

- 四本 Sherlock Holmes 文本来源。
- paragraph splitting、filtering、`(context, target)` 样本构造。
- train / val / test split。
- 已验证 test 集大小为 `663`，正文数值结果都基于这个 test set。

### 4.2 Main Experiment Matrix

本节应写什么：

- 主实验矩阵只放 `E1-E5`。
- `E5W` 只作为 robustness note，不放进主矩阵叙事中心。

本节不要写什么：

- 不要把 retrieval 放进 main experiment matrix。

### 4.3 Training Setup

本节应写什么：

- `num_train_epochs = 3`
- `batch_size = 2`
- `gradient_accumulation_steps = 4`
- `learning_rate = 5e-5`
- `max_length = 512`
- checkpoint 选择基于 validation 指标规则
- `E5` vs `E5W` 用 `validation_main_loss` 选主版本

### 4.4 Evaluation Protocol

本节应写什么：

- automatic metrics：
  - `perplexity`
  - `ROUGE-L`
  - `BERTScore`
- `entity_overlap` 作为 diagnostic metric
- generation 使用 3 个固定 seed
- 所有指标报告 mean；只有 generation-based metrics 报 std
- 明确说明 `perplexity` 对固定 checkpoint 是确定性的

### 4.5 Scope Note

本节应写什么：

- human evaluation deferred
- retrieval appendix 已完成，但仅作为 appendix-level comparison
- 正文主结论只由 `E1-E5` 主线支持

本节不要写什么：

- 不要把 retrieval appendix 写成正文主证据之一。
- 不要把 human evaluation 写成已完成流程。

## 5. Numerical Results

### 5.1 Main Results Table

本节应写什么：

- 主表只放 `E1-E5`
- 建议列：
  - validation main loss
  - validation perplexity
  - test `perplexity_mean`
  - `ROUGE-L_mean ± std`
  - `BERTScore_mean ± std`
  - `entity_overlap_mean ± std`
- `E5W` 单独放 robustness note 或小表

本节可直接按当前结果组织：

- `E1`: val loss `3.1019`, test ppl `27.8093`
- `E2`: val loss `3.0998`, test ppl `27.8216`
- `E3`: val loss `3.0860`, test ppl `27.6525`
- `E4`: val loss `3.2966`, test ppl `32.6382`
- `E5`: val loss `3.1022`, test ppl `28.2869`

本节不要写什么：

- 不要把 retrieval 放进主表。

### 5.2 Pairwise Analysis

本节应写什么：

- 正文只按这 4 组 pairwise comparison 解释：
  - E1 vs E2
  - E2 vs E3
  - E3 vs E4
  - E3 vs E5
- 当前可支撑的主结论方向：
  - `E1 -> E2` 只有轻微改善，主要体现在 overlap-based metrics
  - `E3` 是当前最稳主配置，validation main loss 和 test perplexity 都最好
  - `E4` 明显弱于 full fine-tuning
  - `E5` 未明显优于 `E3`

本节不要写什么：

- 不要横向无差别比较所有实验。
- 不要把很小的自动指标差异夸大成强结论。

### 5.3 E5 Robustness Note

本节应写什么：

- 比较 `E5` 与 `E5W`
- 说明主版本按 `validation_main_loss` 选择
- 明确当前结果：
  - `E5` 的 `validation_main_loss = 3.1022`
  - `E5W` 的 `validation_main_loss = 3.1062`
  - 两者差异小，但 `E5` 更好
- 结论写法建议：
  - `E5W` 只作为 robustness check
  - 不改变“auxiliary ranking 未明显优于 baseline”的主结论

### 5.4 Qualitative Examples

本节应写什么：

- 放 2–3 个例子即可
- 用例子支撑主结论，不要堆长篇文本
- 优先对比：
  - `E3` vs `E4`
  - `E3` vs `E5`
  - 可选：`E1` vs `E2`

### 5.5 Limitations

本节应写什么：

- no human evaluation
- single-domain corpus
- small backbone
- `k=4` structured setting 在 `max_length = 512` 下有非低截断率

这节建议点出的具体 caveat：

- `E3/E5` 的 token budget 显示 truncation rate 不低：
  - train 约 `0.247`
  - eval 约 `0.173`
- 因此可以写：
  - `E3` 依然是最强配置
  - 但其结果是在存在一定截断的条件下得到的

本节不要写什么：

- 不要把 retrieval 写成失败实验的主叙事。
- 不要把未做的人评写成未来一定会补上的承诺句。

## 6. Conclusion

本节应写什么：

- 用一段总结主线结论：
  - structured input 仅有轻微帮助
  - longer context + structured full (`E3`) 最稳
  - LoRA 在本任务下明显退化
  - ranking auxiliary 未证明优于 baseline
- 可以加一句关于 `E5W`：
  - changing the auxiliary weight does not alter the main conclusion
- future work 改成：
  - human evaluation
  - larger backbones
  - alternative coherence objectives
  - improved retrieval design rather than “retrieval completion”

本节不要写什么：

- 不要让 appendix retrieval 改写正文主结论。
- 不要在 conclusion 中把 retrieval 写成 main takeaway。

## 7. Bibliography

本节应写什么：

- 按 ICML references 风格写
- 除 proposal 里的论文外，还要补齐：
  - ROUGE
  - BERTScore
  - LoRA
  - GPT-2 / transformer
  - story generation / coherence
  - retrieval 相关工作（只为 appendix 提供背景）

本节不要写什么：

- 不要只列 proposal 里的最初几篇文献。

## 8. Statement of Individual Contributions

本节应写什么：

- 每位成员 1–2 句
- 写明确的技术贡献
- 用具体工作描述，不写泛泛参与

本节不要写什么：

- 不要写成感谢致辞。

## Appendix Plan

### Appendix A. Retrieval-Augmented Extension

这一节不再是占位，而是已完成的 appendix。

本节应写什么：

- 简短交代 retrieval appendix 的目的：
  - 作为附加对照，而不是主贡献
- 简短交代设置：
  - retrieval model / inference path / appendix-only status
- 放一个小表，建议对比：
  - retrieval
  - `E3`
  - 可选 `E5`
- 当前 appendix 结论可以直接写：
  - retrieval did not outperform the best mainline model
  - therefore it does not change the main conclusions

当前已知 retrieval 结果可作为附录写作依据：

- validation main loss `3.1759`
- validation perplexity `27.4444`
- test perplexity `29.8028`
- ROUGE-L `0.1089`
- BERTScore `0.8325`

本节不要写什么：

- 不要让 retrieval appendix 抢走正文篇幅。
- 不要把 retrieval 放进正文 main results table。

### Appendix B. Additional Experimental Details

本节预留内容：

- token budget / truncation observations
- generation settings
- `E5` vs `E5W` comparison details
- why `E3` still wins despite truncation under `max_length = 512`

### Appendix C. Additional Samples

本节预留内容：

- 更多 qualitative examples（如果后续需要）

## Writing Rules For This Version

- 正文主线任何地方都不要把 retrieval 写成 main evidence。
- 正文可以写：
  - `retrieval is reported only in Appendix A`
  - `the appendix retrieval run does not outperform the best mainline system`
- 不要在 abstract、main results table 中展开 retrieval 数值。
- 可以在正文 very short 地提醒读者 appendix exists，但不要让它打断主线。
- human evaluation 只写 deferred，不要写成已完成。

## Figures / Tables Checklist

- Table 1: dataset / split statistics
- Table 2: main experiment matrix (`E1-E5`)
- Table 3: main results table
- Optional Table 4: `E5` vs `E5W`
- Optional Figure 1: prompt format illustration
- Optional Figure 2: qualitative examples
- Appendix Table A1: retrieval vs `E3` comparison
- Appendix Table B1: token budget / truncation summary

篇幅控制要求：

- 正文总页数上限 `8` 页
- references 不计入页数
- appendix 可扩展
- 正文不能把 appendix 当作藏主结果的地方
