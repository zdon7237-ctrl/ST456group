# ST456 Final Report Outline

本文是最终报告写作大纲，不是最终报告草稿。  
正文主线仅基于已经完成的 `E1-E5` 实验；retrieval appendix 目前只预留结构占位，不写结果与结论；human evaluation 本次不进入实验结果，只在 limitations / future work 中说明 deferred。

## Title

建议标题二选一：

- `Contextual Coherence in Sherlock Holmes Paragraph Continuation via GPT-2 Fine-Tuning`
- `A Comparative Study of Context Modeling and Fine-Tuning Strategies for Sherlock Holmes Paragraph Continuation`

本节应写什么：

- 题目要直接体现任务对象：Sherlock Holmes paragraph continuation。
- 题目要突出主线贡献：context modeling、fine-tuning strategy、auxiliary objective。

本节不要写什么：

- 不要把 retrieval 放进标题。
- 不要把 human evaluation 放进标题。

## Abstract

本节应写什么：

- 用 1 段完成任务、方法、结果、结论的浓缩概述。
- 先交代任务：给定前 `k` 个段落，生成下一段。
- 再交代方法：以 `distilgpt2` 为 backbone，比较输入结构、上下文长度、fine-tuning strategy 和 auxiliary objective。
- 最后交代主结论：
  - `E3` 是当前最稳主配置；
  - `E4` 明显弱于 full fine-tuning；
  - `E5` 未明显优于 `E3`。
- 可在末句保留一句轻量说明：`A retrieval appendix is reserved for later ablation analysis.`

本节不要写什么：

- 不要在 abstract 中写 retrieval 的结果或推测。
- 不要在 abstract 中写 human evaluation。

## 1. Introduction

本节应写什么：

- 介绍 paragraph continuation 的任务难点，重点强调 contextual coherence、character consistency、narrative continuity。
- 说明 Sherlock Holmes 语料适合作为研究对象：同一 narrative world、重复角色、风格相对稳定。
- 明确本项目的核心问题不是 retrieval，而是 deep learning design choices。
- 列出 4 组正文允许的主比较：
  - E1 vs E2：输入结构
  - E2 vs E3：上下文长度
  - E3 vs E4：full vs LoRA
  - E3 vs E5：auxiliary objective
- 用一个简短段落预告 main findings：
  - structured input 只有轻微帮助；
  - longer context + structured full (`E3`) 最稳；
  - LoRA 在本设置下退化；
  - ranking auxiliary 没有带来清晰收益。
- 结尾可写一句：retrieval is reserved as a later appendix experiment and is not part of the current mainline evidence.

本节不要写什么：

- 不要把 retrieval 当作 main contribution。
- 不要提前讨论 appendix 结果。

## 2. Related Work

本节应写什么：

- 按 3 到 4 类相关工作组织：
  - GPT-2 / autoregressive language models
  - story generation / long-form coherence
  - LoRA and parameter-efficient fine-tuning
  - auxiliary objectives for conditional generation
- 每类只综述与本项目主问题直接相关的工作。
- 在结尾点一句：retrieval-related work is acknowledged but retrieval is not the main focus of the present report.

本节不要写什么：

- 不要把 retrieval literature 展开成一个主章节。
- 不要让 related work 比 methodology 更长。

## 3. Methodology / Architecture

### 3.1 Task Formulation

本节应写什么：

- 明确任务是 conditional next-paragraph generation。
- 输入是前 `k` 个段落，输出是目标下一段。

本节不要写什么：

- 不要在这里展开所有实验结果。

### 3.2 Backbone

本节应写什么：

- 说明 backbone 为 `distilgpt2`。
- 简述选择理由：小模型、Colab 可训练、适合 controlled comparison。

### 3.3 Input Representation

本节应写什么：

- `plain` vs `structured`
- structured 输入如何加入段落标记。

### 3.4 Context Window

本节应写什么：

- `k=2` vs `k=4`
- 强调这是上下文信息量的核心对比。

### 3.5 Fine-Tuning Strategy

本节应写什么：

- full fine-tuning
- LoRA

### 3.6 Auxiliary Objective

本节应写什么：

- next-paragraph ranking objective
- `E5` 与 `E5W` 的区别只是 `aux_weight`

本节不要写什么：

- 不写 retrieval 机制细节。
- 如果必须提 retrieval，只能写：`retrieval is left to appendix-level extension.`

## 4. Training Methods and Experimental Setup

### 4.1 Dataset and Preprocessing

本节应写什么：

- 四本 Sherlock Holmes 文本来源。
- paragraph splitting、filtering、`(context, target)` 样本构造。
- train / val / test split。

### 4.2 Main Experiment Matrix

本节应写什么：

- 放 `E1-E5` 主实验表。
- `E5W` 只作为 robustness note，不放进主矩阵叙事中心。

本节不要写什么：

- 不要把 retrieval 放进 main experiment matrix。

### 4.3 Training Setup

本节应写什么：

- `num_train_epochs = 3`
- `batch_size = 2`
- `gradient_accumulation_steps = 4`
- `learning_rate = 5e-5`
- `max_length = 512`
- checkpoint 选择基于 validation 指标规则。

### 4.4 Evaluation Protocol

本节应写什么：

- 只写 automatic metrics：
  - `perplexity`
  - `ROUGE-L`
  - `BERTScore`
- `entity_overlap` 作为 diagnostic metric。
- generation 使用 3 个固定 seed。
- 所有指标报告 mean；只有 generation-based metrics 报 std。

### 4.5 Scope Note

本节应写什么：

- human evaluation deferred。
- retrieval appendix 尚未执行完成，因此不纳入当前数值结论。

本节不要写什么：

- 不要把 retrieval 说成 negative result。
- 不要把 human evaluation 写成已完成流程。

## 5. Numerical Results

### 5.1 Main Results Table

本节应写什么：

- 主表只放 `E1-E5`。
- 建议列：
  - validation main loss
  - validation perplexity
  - test `perplexity_mean`
  - `ROUGE-L_mean ± std`
  - `BERTScore_mean ± std`
  - `entity_overlap_mean ± std`
- `E5W` 可以放脚注、旁注，或者单独小表。

本节不要写什么：

- 不要把 retrieval 放进主表。

### 5.2 Pairwise Analysis

本节应写什么：

- 正文只能按这 4 组 pairwise comparison 解释：
  - E1 vs E2
  - E2 vs E3
  - E3 vs E4
  - E3 vs E5
- 当前可支撑的主结论方向：
  - `E3` 是当前最稳主配置；
  - `E4` 明显弱于 full fine-tuning；
  - `E5` 未明显优于 `E3`；
  - `E1 -> E2` 只有轻微改善。

本节不要写什么：

- 不要横向无差别比较所有实验。
- 不要把很小的自动指标差异夸大成强结论。

### 5.3 E5 Robustness Note

本节应写什么：

- 比较 `E5` 与 `E5W`。
- 说明主版本按 `validation_main_loss` 选择。
- 说明 `E5W` 只作为 robustness check，不进入主叙事中心。
- 当前写法建议：
  - `E5W` 与 `E5` 差异很小；
  - 不改变“auxiliary ranking 未明显优于 baseline”的主结论。

### 5.4 Qualitative Examples

本节应写什么：

- 放 2–3 个例子即可。
- 例子用途是支撑主结论，而不是堆长篇文本。
- 推荐对比 `E3` 与 `E4`，或 `E3` 与 `E5`。

### 5.5 Limitations

本节应写什么：

- no human evaluation
- no completed retrieval appendix yet
- single-domain corpus
- small backbone

本节不要写什么：

- 不要把 retrieval 写成失败实验。
- 不要把未做的人评写成未来一定会补上的承诺句。

## 6. Conclusion

本节应写什么：

- 用一段总结主线结论：
  - structured input 仅有轻微帮助；
  - longer context + structured full (`E3`) 最稳；
  - LoRA 在本任务下退化；
  - ranking auxiliary 未证明优于 baseline。
- 用一段写 future work：
  - retrieval appendix completion
  - human evaluation
  - larger backbones
  - alternative coherence objectives

本节不要写什么：

- 不要在 conclusion 中引用 retrieval 结果。

## 7. Bibliography

本节应写什么：

- 按 ICML references 风格写。
- 除 proposal 里已有论文外，还应补齐任务与方法相关引用。

本节不要写什么：

- 不要只列 proposal 里的最初几篇文献。

## 8. Statement of Individual Contributions

本节应写什么：

- 每位成员 1–2 句，写明确的技术贡献。
- 用具体工作描述，不写泛泛参与。

本节不要写什么：

- 不要写成感谢致辞。

## Appendix Plan

### Appendix A. Retrieval-Augmented Extension (Reserved)

当前只写 2–4 句占位说明，内容固定为：

- retrieval was planned as an appendix-level ablation
- experiment execution has not been completed yet
- no retrieval result is used in the current main conclusions
- this appendix is intentionally reserved for later completion

禁止事项：

- 不放空表
- 不放假结果
- 不写推测性分析
- 不在正文提前引用 appendix 结果

### Appendix B. Additional Experimental Details

本节预留内容：

- token budget / truncation observations
- generation settings
- `E5` vs `E5W` comparison details
- optional extra examples

### Appendix C. Additional Samples

本节预留内容：

- 更多 qualitative examples（如果后续需要）

## Writing Rules For This Version

- 正文任何地方都不要写“retrieval improved / failed / compared against mainline”。
- 可以写：
  - `retrieval is reserved as an appendix ablation`
  - `retrieval results are not included in the present report`
- 不要在 abstract、main results table、conclusion 中引用 retrieval。
- 不要在 limitations 里把 retrieval 说成 negative result，只能写 not yet executed / reserved for appendix。

## Figures / Tables Checklist

- Table 1: dataset / split statistics
- Table 2: main experiment matrix (`E1-E5`)
- Table 3: main results table
- Optional Table 4: `E5` vs `E5W`
- Optional Figure 1: prompt format illustration
- Optional Figure 2: qualitative examples
- Appendix A placeholder only for retrieval

篇幅控制要求：

- 正文总页数上限 `8` 页
- references 不计入页数
- appendix 可扩展，但正文不能把 appendix 当作藏主结果的地方
